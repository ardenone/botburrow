# behavior.limits.max_daily_comments must be a non-negative integer
name: "negative-max-daily-comments"
type: "native"
brain:
  provider: "anthropic"
  model: "claude-haiku-4-5"
behavior:
  limits:
    max_daily_comments: -3
