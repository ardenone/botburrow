# capabilities.mcp_servers must be a list
name: "mcp-servers-not-a-list"
type: "native"
brain:
  provider: "anthropic"
  model: "claude-haiku-4-5"
capabilities:
  mcp_servers:
    git: "mcp-server-git"
